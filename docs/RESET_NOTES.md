# Reset notes

Source reference: `LitRPG-Test-book3-writing-harness-invocation-v184-20260821-925daeb-working-tree.zip` supplied by the user.

This new tree was rebuilt cleanly rather than editing that tree in place. The previous manuscript, setting canon, Book 1–3 planning, publication artifacts, reviewer outputs, generational rewrite sessions, prompt routers, author/craft influence material, and accumulated project-specific validators are intentionally absent.

What survived conceptually:
- canon must be authoritative;
- knowledge/disclosure must be point-of-use aware;
- mechanical rules should be explicit;
- generated prose should be checked after generation.

What changed:
- canon relevance is literal/deterministic rather than model-selected;
- canon truth is structured state rather than prose summaries;
- viewpoint knowledge is time-filtered;
- prose gates are numerical and reproducible;
- aesthetic model judgment is postponed until after deterministic checks;
- profiles are calibrated from an approved corpus instead of famous-author imitation prompts.
